<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boardQuery">
	
	<!-- select -->
	<select id="detailBoard"  resultType="hashmap" parameterType="hashmap">
		select
		CONTENTS_ID,
		MEMBER_ID,
		TITLE,
		CONTENTS,
		REG_DT
		from
		BG_BOARD_CONTENTS
	</select>
	
	
	<!-- insert -->
	<insert id="insertBoard"  parameterType="hashmap">
		INSERT INTO BG_BOARD_CONTENTS (
		BOARD_ID,
		MEMBER_ID,
		TITLE,
		CONTENTS
		)
		VALUES (
        #{ board_id },
        #{ id },
        #{ board_title },
        #{ board_contents }
		)
	
	</insert>

</mapper>
